# Локальные данные

Положите исходный TTS-датасет в `raw/`, затем запустите `src/prepare_dataset.py`. Подготовленный датасет появится в `processed/`.

Тяжёлые аудиофайлы не предназначены для хранения в git.
